//      Options:

//      slidetime - default = 5000 (number of miliseconds between each slide)
//      effect - default = "fade" (optionally shrink, slide, blind)
//      arrows - default = "show" (optionally "hide" the previous and next arrows)
//      arrowSize - default = 60  (size in pixels of the arrows)


$( ".carousel" ).myCarousel({
    slidetime: 2500,
    effect: 'blind',
  	arrows: 'show',
  	arrowSize: 80
})